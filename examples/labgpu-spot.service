[Unit]
Description=labgpu GPU idleness monitor for spot lending
After=docker.service
Requires=docker.service

[Service]
ExecStart=/usr/local/bin/labgpu-spot -c /etc/labgpu/spot.toml daemon
Restart=on-failure
RestartSec=10

[Install]
WantedBy=multi-user.target
